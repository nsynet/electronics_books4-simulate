//-----------------------------------------------------------------
//  名称: PCF8583实时时钟显示程序
//-----------------------------------------------------------------
//  说明: 从PCF8583中读取日历时钟数据并刷新显示于LCD.
//
//-----------------------------------------------------------------
#include <Wire.h>
#include <LiquidCrystal.h>
LiquidCrystal lcd(8, 9, 10, 4, 5, 6, 7);
//PCF8583器件7位地址
#define PCF8583_ADDR 0x50
char buf1[12] = "DATE: 00/00";
char buf2[16] = "TIME: 00-00-00";
//0,1,2,3,4,5,6分别对应周日,周一至六
char WEEK[][4] = {"SUN","MON","TUS","WEN","THU","FRI","SAT"};
char disp_buff[10];
//所读取的PCF8583数据及转换后的日期时间数据
uint8_t PCF8583_DATA[5],DateTime[7];
//-----------------------------------------------------------------
// 初始配置
//-----------------------------------------------------------------
void setup() {
 
}
//-----------------------------------------------------------------
// 主循环
//-----------------------------------------------------------------
void loop() {
 	
}
//-----------------------------------------------------------------
// 日期与时间值转换为数字字符
//-----------------------------------------------------------------
void Format_DateTime(uint8_t d, uint8_t *a){ 
	*a = (d >> 4) + '0'; *(a+1) = (d & 0x0F) + '0';
}
//-----------------------------------------------------------------
// 从PCF8583指定地址读多字节到buf缓冲
//-----------------------------------------------------------------
void Read_PCF8583(uint8_t addr,uint8_t *buf,uint8_t n) {
 
}
//-----------------------------------------------------------------
// 读取7字节PCF8583时钟数据并整理到DateTime数组
//-----------------------------------------------------------------
void GetDateTime(){
 
}