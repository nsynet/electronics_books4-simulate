//-----------------------------------------------------------------
//  名称: TIMER2控制门铃声音输出
//-----------------------------------------------------------------
//  说明: 按下按键时蜂鸣器发出叮咚的门铃声
//
//-----------------------------------------------------------------
#define Door		19		//19号引脚连接蜂鸣器
#define DoorBell	PC5		//19号引脚对应Atmega328的PC5引脚
#define K1			0		//按键1连接引脚0
#define K2			1		//按键2连接引脚1
#define K1_DOWN()	(digitalRead(K1) == 0) //按键判断
#define K2_DOWN()	(digitalRead(K2) == 0) //按键判断
//-----------------------------------------------------------------
// 初始配置
//-----------------------------------------------------------------
void setup() {

}
//-----------------------------------------------------------------
// 主循环
//-----------------------------------------------------------------
void loop() {
	if(K1_DOWN()) {					//按下K1时启动定时器

	}
	if(K2_DOWN()) {					//按下K1时启动定时器

	}
}
//-----------------------------------------------------------------
// Timer2定时中断（使用tone函数时可在TIMER2_COMPA_vect前加下划线暂时屏蔽）
//-----------------------------------------------------------------
ISR (TIMER2_COMPA_vect) {	
								//使能中断
}

