//-----------------------------------------------------------------
//  名称: 按键控制数码管闪烁增减显示
//-----------------------------------------------------------------
//  说明: 通过按键加减数码管所显示的数字,调节过程中数码管闪烁显示.
//		  确定后恢复正常显示,按下取消时将恢复到调节之前的值.
//
//-----------------------------------------------------------------
//调节按键定义
#define K1_DOWN() (digitalRead(14) == 0)  //加
#define K2_DOWN() (digitalRead(15) == 0)  //减
#define K3_DOWN() (digitalRead(16) == 0)  //确定
#define K4_DOWN() (digitalRead(17) == 0)  //取消
//0~9的共阳数码管段码表
const uint8_t CA_SEGCODE[] =
 {0xC0,0xF9,0xA4,0xB0,0x99,0x92,0x82,0xF8,0x80,0x90};
uint16_t Count = 3057;			//本例所设置的显示初值
uint16_t Temp;					//临时变量
uint8_t  array[] = {0,0,0,0,};	//保存数位分解结果的数组
uint8_t  Adjust = 0;			//标识当前是否处理调节状态
uint8_t  Pre_key = 0xFF;		//保存上次按键状态
uint8_t i,j,t;
//-----------------------------------------------------------------
// 配置函数
//-----------------------------------------------------------------
void setup() {

}
//-----------------------------------------------------------------
// 主循环
//-----------------------------------------------------------------
void loop() {
	//1.正常刷新显示---------------------------------------------

	//2.处于调节状态---------------------------------------------

}
//-----------------------------------------------------------------
// 数位分解函数(分解为4个数位)
// 使用循环减法的分解速度优于使用/与%分解
//-----------------------------------------------------------------
void INT_TO_4Digit(uint16_t n) {

}
//-----------------------------------------------------------------
// 按键处理函数
//-----------------------------------------------------------------
void key_handle() {




}

