//-----------------------------------------------------------------
//  名称: INT0中断计数
//-----------------------------------------------------------------
//  说明: 每次按下计数键时触发INT0中断,中断程序累加计数,
//		计数值显示在三只数码管上,按下清零键时数码管清零.
//
//-----------------------------------------------------------------
#define K1 2	//中断触发引脚定义（K1-Arduino INT0）
#define K2 3	//本例定义为普通输入引脚
//-----------------------------------------------------------------
//0~9的共阳数码管段码表（最后一位为黑屏段码）
const uint8_t CA_SEGCODE[] =
{ 0xC0,0xF9,0xA4,0xB0,0x99,0x92,0x82,0xF8,0x80,0x90,0xFF};
//-----------------------------------------------------------------
//计数器值分解为各待显示数位(3位)
uint8_t Display_Buffer[3] = {0,0,0};
//三只分立式数码管段码引脚定义
uint8_t LED1_pins[] = { 22, 23, 24, 25, 26, 27, 28, 29 };	
uint8_t LED2_pins[] = { 30, 31, 32, 33, 34, 35, 36, 37 };	
uint8_t LED3_pins[] = { 38, 39, 40, 41, 42, 43, 44, 45 };	
//-----------------------------------------------------------------
volatile int Count = 211;	//计数变量（初值暂设为211）
uint8_t i,j;
//-----------------------------------------------------------------
// 初始配置
//-----------------------------------------------------------------
void setup() {
	//将三只LED数码管的引脚全部定义为输出
	for (i = 0; i < 8; i++)	pinMode(LED1_pins[i],OUTPUT);
	for (i = 0; i < 8; i++)	pinMode(LED2_pins[i],OUTPUT);
	for (i = 0; i < 8; i++)	pinMode(LED3_pins[i],OUTPUT);	



}
//-----------------------------------------------------------------
// 主循环
//-----------------------------------------------------------------
void loop() {

}
//-----------------------------------------------------------------
// 在数码管上显示计数值
//-----------------------------------------------------------------
void Refresh_Display() {

}
//-----------------------------------------------------------------
// 段码输出函数
//-----------------------------------------------------------------
void SEG_OUTPUT(uint8_t LED_Pin[], uint8_t x) {

}
//-----------------------------------------------------------------
// 中断服务函数：累加计数
//-----------------------------------------------------------------
void ISRINT0_Count() {

}