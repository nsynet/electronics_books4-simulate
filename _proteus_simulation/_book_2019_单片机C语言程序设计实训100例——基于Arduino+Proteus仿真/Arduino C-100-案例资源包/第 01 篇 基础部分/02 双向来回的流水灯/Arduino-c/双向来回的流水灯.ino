//-----------------------------------------------------------------
//  名称: 双向来回的流水灯
//-----------------------------------------------------------------
//  说明: 程序利用递增、递减循环控制8只LED来回滚动显示。
//
//-----------------------------------------------------------------
// 用数组指定LED所连接的引脚
const uint8_t ledPins[] = {  };
uint8_t i;
//-----------------------------------------------------------------
// 初始配置
//-----------------------------------------------------------------
void setup() {
	//将ledPins数组指定的引脚逐一设为输出
	for (i = 0; i < 8; i++) { 

	}
}
//-----------------------------------------------------------------
// 主循环
//-----------------------------------------------------------------
void loop() {
	//0～7号索引指定的引脚逐一置高电平,每点亮一个LED后延时50ms。
	for (i = 0; i <= 7;  i++) {

	}

	for (i = 7; i != 0xFF; i--) {

	}
}